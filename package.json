{
  "name": "ocean-ai-backend",
  "version": "1.0.0",
  "description": "Backend server for Ocean AI - keeps the Google API key private and proxies chat/image requests.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
